include includes/header

// Navbar
body
  include includes/navbar

  include mixin/mail_form

  .p-page
    main.l-main
      .p-cover-header.p-cover-header--news
        h2.p-cover-header__title NEWS

      .container-fluid
        .p-block--sm
          .row.p-news-cards
            include mixin/news_card
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body)  
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body)  
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body) 
          .row
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body)  
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body)  
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body) 
          .row
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body)  
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body)  
            .col-sm-4
              .p-news-card.p-news-card--twitter
                +news_card(
                news.category, 
                news.date,
                news.header,
                news.body) 

  // Footer
  #meetus
  include includes/footer

  // generated by webpack
  script(type="text/javascript", src='/js/low_priority_index.js')
