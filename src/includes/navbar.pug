extends _navbar

block links
  #navbar.navbar-collapse.collapse
    <h3 class="navbar__header">MENU</h3>
    ul.nav.navbar-nav
      li
        a.nav__about(href='/about.html') #{navbar.about}
      li
        a.nav__whitepaper(href=whitepaper.url, target="_blank") #{navbar.whitepaper}
      li
        a.nav__community(href='/community.html') #{navbar.community}
      li
        a.nav__product(href='/product.html') #{navbar.product}
      li
        a.nav__team(href='/team.html') #{navbar.team}
      li
        a.nav__roadmap(href=sns_url.trello, target="_blank") #{navbar.roadmap}
      li
        a.nav__news(href='/news.html') #{navbar.news}
      li.u-md-none
        .c-select
          span.c-select__lang.js-select-lang
          .c-select__opt
            .c-select__bg
            ul.c-select__options
              li.c-select__option EN
              li.c-select__option JA

